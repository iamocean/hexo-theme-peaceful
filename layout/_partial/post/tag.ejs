<% if (item.tags && item.tags.length){ %>
  <%
  var tags = [];
  item.tags.forEach(function(tag){
    tags.push('<a href="' + config.root + tag.path + '" class="item-tags">#' + tag.name + '</a>');
  });
  %>
  <div class="post-tags">
    <!-- 去掉, -->
    <%- tags.join(' ') %>
  </div>
<% } %>
